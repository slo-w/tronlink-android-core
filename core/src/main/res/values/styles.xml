<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <!-- horizontal line-->
    <style name="action_bar_line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/gray_ee</item>
    </style>

    <!--Dialog-->
    <style name="loading_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- no window frame -->
        <item name="android:windowIsFloating">true</item>
        <!-- float above the activity -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- translucency -->
        <item name="android:windowNoTitle">true</item>
        <!-- no title bar -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- custom background -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- dim behind -->

        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="loading_dialog_2" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- no window frame -->
        <item name="android:windowIsFloating">true</item>
        <!-- float above the activity -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- translucency -->
        <item name="android:windowNoTitle">true</item>
        <!-- no title bar -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- custom background -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- dim behind -->

        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="dialog_bright" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- no window frame -->
        <item name="android:windowIsFloating">true</item>
        <!-- float above the activity -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- translucency -->
        <item name="android:windowNoTitle">true</item>
        <!-- no title bar -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- custom background -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- dim behind -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
    </style>
</resources>
